# 探测包与卫星节点遥测流程优化方案

## 一、探测包维护的信息

* **待测节点 `next_telemetry_node`**：表示当前真正需要执行遥测的节点。
* **SRv6 路径 `Segment List`**：表示探测包从当前节点到待测节点的显式转发路径。
* **已测量节点的链路信息 `telemetry_record`**：记录已经完成测量节点的邻接链路状态。
* **跳数限制 `Hop Limit`**：限制探测包最大转发跳数，防止异常情况下无限转发。

---

## 二、卫星节点维护的信息

* **哈密顿圈的路线图**：用于确定当前节点测量完成后的下一个待测节点。
* **拓扑的邻接关系**：用于路径计算，生成到待测节点的 SRv6 路径。
* **本节点 ID**：用于和探测包中的 `next_telemetry_node` 进行比较。
* **本地邻接链路实时状态检测能力**：用于在当前节点执行遥测时获取相邻链路实时状态。

---

## 三、执行流程

1. **初始化**：初始化探测包，设置第一个待测节点 `next_telemetry_node`。
2. **初始路径规划**：根据当前节点和 `next_telemetry_node`，结合拓扑邻接关系计算一条无环路径，并写入探测包的 SRv6 Segment List。
3. **网络转发**：探测包按照 SRv6 路径在网络中转发，同时每经过一跳，`Hop Limit` 递减。
4. **目标读取**：当探测包到达某个卫星节点时，该节点读取包中的 `next_telemetry_node`。
5. **身份校验**：节点使用本节点 ID 判断是否等于 `next_telemetry_node`。
6. **中继（未命中目标）**：如果不相等，说明当前节点只是中继节点：不执行遥测，不更新链路信息，不更新待测节点，继续按照 SRv6 路径转发。
7. **执行遥测（命中目标）**：如果相等，说明当前节点是本轮待测节点：执行遥测，检测本地邻接链路实时状态，将测量结果写入 `telemetry_record`。
8. **更替下一目标**：当前节点根据本地维护的哈密顿圈路线图，确定下一个待测节点：`next_telemetry_node = hamiltonian_successor[本节点 ID]`。
9. **直连链路校验**：当前节点利用“本地邻接链路实时状态检测能力”，优先检查到新 `next_telemetry_node` 的直连物理链路状态。如果该直连下一跳链路存在且正常，则不触发路径重计算；直接修改探测包头部目的地址，沿哈密顿圈物理链路直接转发给下一跳。
10. **动态路径重计算**：如果第 9 步检测到前往新待测节点的直连链路不存在或已断开，或者在普通的标准转发过程中检测到前方链路断开，则触发路径重计算：此时不更新待测节点，仍然以当前 `next_telemetry_node` 为目标重新规划路径，并更新探测包中的 Segment List。
11. **循环遍历**：探测包重复上述过程，直到哈密顿圈中的所有待测节点完成测量。
12. **探测包回收**：最后一个节点测量完成后，根据哈密顿圈路线图将 `next_telemetry_node` 设置为源节点或回收节点，探测包沿重新计算的 SRv6 路径或正常的直连链路返回。
13. **结果交互**：探测包回到源节点后，源节点读取 `telemetry_record`，得到整轮探测结果。

---

## 四、仿真方案设计

### 仿真工具

采用 **Mininet + Linux SRv6 ** 进行验证

实验入口为 `emulation/mininet_srv6_lab.py`，配置文件为 `config/mininet_srv6.toml`。仿真环境使用 `iproute2` 安装 SRv6 路由，使用 `tc netem` 注入链路时延和链路中断，使用 UDP 探测包和节点 agent 记录遥测结果。

### 网络拓扑建模

仿真将低轨卫星星座抽象为二维网格拓扑：

* 每个节点表示一颗卫星。
* 纵向链路表示同轨道面内的星间链路。
* 横向链路表示相邻轨道面之间的星间链路。
* 拓扑中的每条链路均按无向边处理，链路记为 `(min(u, v), max(u, v))`。

在 Mininet SRv6 实验中，默认使用 `4 x 4` 星座拓扑，共 16 个节点。每个 Mininet 节点对应一个卫星路由节点 `r0, r1, ..., r15`，节点之间通过 veth 链路相连，并在每个节点内启用 IPv6 转发和 SRv6 功能。哈密顿圈由网格蛇形遍历方式生成，保证每个节点在一轮探测中恰好被选为一次待测节点。

### 时延模型

仿真支持两类链路时延模型：

* **固定时延模型**：为每条链路设置固定时延，适合基础连通性验证。
* **LEO 类周期时延模型**：将同轨道面链路设置为相对稳定的时延，将跨轨道面链路设置为随时间槽变化的周期性时延，用于近似低轨卫星相对运动导致的传播时延变化。

路径规划时，节点读取当前时间槽对应的链路权重，并使用最短路算法计算从当前节点到 `next_telemetry_node` 的路径。若时间槽变化导致当前路径代价明显变差，可触发软重规划，以降低总转发时延。

### 链路故障模型

Mininet SRv6 实验采用可控动态故障模型：

* 通过 `tc netem loss 100%` 模拟链路不可用。
* 可设置指定链路在指定时间区间内断开，例如 `9 <-> 5` 链路在 5s 到 15s 之间断开。
* 也可启用 `rotating_seam` 动态拓扑模型，使跨轨道面链路的不可用区域随时间槽移动。

### 探测与重规划方法

仿真中的每个节点维护基础拓扑、哈密顿圈后继表和已观测链路状态表。探测包到达节点后按如下方法处理：

1. 若当前节点不是 `next_telemetry_node`，则只作为中继节点转发探测包，不写入遥测记录。
2. 若当前节点是 `next_telemetry_node`，则读取本节点所有邻接链路状态，将结果写入 `telemetry_record`。
3. 当前节点根据哈密顿圈后继表选择下一待测节点。
4. 若当前节点到下一待测节点存在直连链路且链路状态正常，则直接沿该物理链路转发，不重新计算 SRv6 路径。
5. 若直连链路断开，或者当前 SRv6 路径的下一跳链路断开，则把该链路标记为 `DOWN`，从估计拓扑中删除，并对同一个 `next_telemetry_node` 重新计算路径。
6. 若新的估计拓扑中目标不可达，则本轮实验记为 `unreachable`。
7. 若探测包跳数超过 `max_hop`，则认为可能存在异常转发或环路，实验记为 `partial result`。

路径计算使用 Dijkstra 最短路算法。估计拓扑记为：

```text
Ge(t) = G0 - recent_down_edges(t)
```

其中 `G0` 为基础拓扑，`recent_down_edges(t)` 为探测包在遥测和转发失败检查中已经发现的断开链路集合。若某条链路后续被观测为恢复，则该链路可重新加入估计拓扑。

### SRv6 映射方法

在 Mininet SRv6 仿真中，算法输出的路径会被转换为 SRv6 Segment List：

* 每个卫星节点分配一个 Node SID。
* 当前探测源节点根据规划路径安装 `encap seg6 mode encap` 策略。
* 中间节点安装 `seg6local action End`，用于处理转发 SID。
* 目标节点安装 `seg6local action End.DT6 table 254`，用于完成解封装并交给本地 UDP agent。
* 每次目标更新或路径重规划后，重新安装当前节点到目标节点的 SRv6 策略。

节点 agent 监听 UDP 探测包，收到探测包后记录当前节点、目标节点、路径、跳数、邻接链路观测结果等信息，并写入 `logs/mininet/<run>/agent_r<node>.jsonl`。

### 评价指标

实验统计以下指标：

* **finished**：成功完成全网遥测并返回源节点的实验数量。
* **success rate**：成功完成实验数量占总实验数量的比例。
* **unreachable**：由于断链导致待测节点在估计拓扑中不可达的实验数量。
* **mean hops**：成功实验中探测包平均转发跳数。
* **mean delay**：成功实验中探测包累计链路时延的平均值。
* **mean active down edges**：实验过程中平均处于断开状态的链路数量。
* **hop limit hit**：探测包是否因为超过跳数限制而终止，用于判断是否存在环路或异常转发。

通过对比不同故障链路、故障持续时间和动态拓扑模型下的上述指标，可以评估算法在链路故障增强时的鲁棒性和额外转发代价

---

## 五、测试结果

Mininet SRv6 实验需要从 `logs/mininet/<run>/` 中读取运行结果，并重点核对以下内容：

* `traversal_events.jsonl`：确认探测包是否完成所有待测节点遍历，是否发生重规划，是否命中 `max_hop`。
* `policy_updates.jsonl`：确认每次目标更新或重规划后，当前节点是否安装了新的 SRv6 Segment List。
* `tc_updates.jsonl`：确认链路故障是否按配置时间正确注入和恢复。
* `agent_r<node>.jsonl`：确认目标节点是否收到 UDP 探测包，并记录本地邻接链路遥测结果。

在默认 `4 x 4` 星座、`9 <-> 5` 链路 5s 到 15s 断开的场景下，应重点观察探测包到达 `r9` 后是否发现到 `r5` 的直连链路不可用，并重新规划到同一待测节点 `r5` 的替代 SRv6 路径。若探测包最终完成全网遍历并返回源节点，且未达到 hop limit，则说明动态重规划机制有效，没有出现环路。
