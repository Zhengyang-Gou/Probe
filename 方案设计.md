# 方案设计

## 1. 方案概述

全局访问顺序和局部路径选择分离：

1. 给出稳定的全局访问顺序（哈密顿圈）
2. Dijkstra 在当前估计拓扑和当前时隙时延下计算实际路径
3. 链路观测只来自探针到达节点时看到的邻接链路
4. 近期 DOWN 链路从基础拓扑中临时移除，形成估计拓扑
5. 下一跳不可用时执行硬重规划；时隙变化或链路恢复时执行带阈值的软重规划
6. 所有节点访问完成且探针回到根节点时返回 `FINISHED`

该方案不使用未来链路通断预测，只依赖当前观测、有限时间窗口内的历史观测和周期性时延表

## 2. 基础模型

### 2.1 基础拓扑

基础拓扑表示为：

```text
G0 = (V, E0)
```

其中 `V` 是节点集合，`E0` 是无向边集合

`Topology` 提供以下能力：

- 查询节点邻居
- 判断边是否存在
- 基于待移除边集合生成新的拓扑

### 2.2 Cycle Route

遍历顺序由 `cycle_route` 指定，对网格拓扑，提供：

```python
make_grid_hamiltonian_cycle(rows, cols)
```

该函数为偶数行矩形网格生成一条以 `0` 为根的 S 型哈密顿圈，route 必须满足：

- 包含根节点 `root`
- 覆盖基础拓扑中的所有节点
- 可以在末尾重复根节点，用于表达闭合回路

哈密顿圈只决定下一个访问目标，不强制探针沿 route 的边逐跳移动，实际转发路径始终由 Dijkstra 根据当前估计拓扑计算

### 2.3 链路状态

系统没有全局链路通断预测，某条基础链路没有有效观测时，默认视为可用

链路状态规则为：

```text
有效 DOWN 观测 -> 不可用
有效 UP 观测   -> 可用
无有效观测     -> 默认可用
```

### 2.4 时延模型

链路时延由周期性 `DelayTable` 给出。时间 `t` 映射到时隙：

```text
slot(t) = floor(t) mod period_slots
```

路径规划使用当前时隙下的边权：

```text
w_t(u, v) = DelayTable[slot(t)][u][v]
```

因此，同一条路径在不同时隙可能具有不同代价，时隙变化时，系统会评估是否值得切换路径

## 3. 核心数据结构

### 3.1 ProbeState

`ProbeState` 保存探针运行状态：

- `root`：根节点
- `current_node`：当前节点
- `visited`：已访问节点集合
- `current_path`：当前计划路径
- `path_index`：探针在计划路径中的位置
- `hop_count`：已转发跳数
- `last_slot`：上一次处理到达事件时的时隙
已访问节点仍可作为中继节点，但不会再作为 cycle 访问目标

### 3.2 LinkObservationTable

`LinkObservationTable` 保存无向边的最新观测。每条观测包含：

- `edge`
- `state`：`UP` 或 `DOWN`
- `observed_time`：观测时间
- `ttl`：有效期

过期时间为：

```text
expires_at = observed_time + ttl
```

当 `now >= expires_at` 时，观测被删除，链路回到默认可用状态。

## 4. 估计拓扑

每次探针到达 `current_node`，系统先观测当前节点的邻接链路：

```text
state = provider(current_node, neighbor, now)
```

新的观测覆盖同一条边的旧观测，随后系统删除过期观测，并提取所有有效 DOWN 边：

```text
recent_down_edges(t)
```

估计拓扑定义为：

```text
Ge(t) = G0 - recent_down_edges(t)
```

如果 DOWN 链路后来被观测为 UP，或 DOWN 观测过期，该链路会重新进入 `Ge(t)`

## 5. Cycle-Guided 路径规划

路径规划分两步：先选择 cycle 目标，再计算到该目标的最短路径

### 5.1 目标选择

若仍有未访问节点，系统按 `cycle_route` 顺序扫描，选择第一个当前可达的未访问节点作为目标：

```text
for target in cycle_route:
    if target not in visited and path(current_node, target) exists:
        use target
```

如果 cycle 中靠前的未访问节点暂时不可达，系统继续尝试后续未访问节点，若所有未访问节点在 `Ge(t)` 中都不可达，则返回 `TEMPORARILY_UNREACHABLE`。

若所有节点都已访问，但探针尚未回到根节点，目标切换为根节点 `root`

### 5.2 实际路径

选定目标后，系统调用：

```text
shortest_path(Ge(t), delay_table, slot(t), current_node, target)
```

该函数使用 Dijkstra 计算当前时隙下的最小时延路径，cycle route 不限制路径形状，已访问节点也可以作为中继节点

### 5.3 路径代价

路径 `p = [v0, v1, ..., vk]` 在时隙 `s` 下的代价为：

```text
cost(p, s) = sum DelayTable[s][vi][vi+1]
```

软重规划会比较当前剩余路径代价和候选路径代价

## 6. 重规划机制

### 6.1 硬重规划

硬重规划处理当前路径无法继续执行的情况，触发条件包括：

- 当前没有计划路径
- 当前路径已经没有下一跳
- 下一跳边不在估计拓扑 `Ge(t)` 中

硬重规划的目标规则为：

- 未完成遍历：选择 cycle 顺序中第一个当前可达的未访问节点
- 已完成遍历：规划返回根节点

规划成功后，系统会替换 `current_path`，把 `path_index` 置为 `0`，并更新 `last_slot`。

### 6.2 软重规划

软重规划用于当前路径仍可执行但可能不再合适的情况。触发条件包括：

- 当前时隙相比上一次到达事件发生变化。
- 有 DOWN 观测消失，表示链路恢复或观测过期。

系统计算：

```text
old_cost = 当前剩余路径代价
candidate_cost = cycle 目标候选路径代价
```

设 `alpha` 为切换阈值，默认 `0.85`，只有当：

```text
candidate_cost < alpha * old_cost
```

才替换当前路径，该阈值避免因轻微时延波动频繁切换

## 7. 链路恢复

链路恢复有两种来源：

- 新的 UP 观测覆盖旧的 DOWN 观测。
- DOWN 观测超过 TTL 后过期。

恢复后，该边会从 `recent_down_edges(t)` 中移除，并重新加入估计拓扑。系统随后进行软重规划检查；若恢复后的路径明显更优，探针会切换到新路径。

## 8. 到达事件流程

`on_probe_arrival` 的整体流程如下：

1. 更新探针当前节点和当前时隙
2. 将当前节点加入 `visited`
3. 观测当前节点的所有邻接链路
4. 移除过期观测
5. 构建估计拓扑 `Ge(t)`
6. 若所有节点已访问且当前节点为根节点，返回 `FINISHED`
7. 若跳数达到 `max_hop`，返回 `PARTIAL_RESULT`
8. 若当前路径不可继续，执行硬重规划
9. 否则在必要时执行软重规划
10. 取当前路径下一跳，更新路径索引和跳数
11. 返回 `RUNNING` 和下一跳

## 测试
| 断链概率 |  成功率 |   平均跳数 | 平均实际延迟 | 平均完成时间 | 平均活跃断链数 | 最大活跃断链数 |
| ---: | ---: | -----: | -----: | -----: | ------: | ------: |
|   0% | 100% | 100.00 | 124.31 |  50.50 |    0.00 |    0.00 |
|  10% | 100% | 104.05 | 132.39 |  52.52 |    3.45 |    6.96 |
|  20% | 100% | 108.16 | 140.30 |  54.58 |    6.81 |   12.28 |