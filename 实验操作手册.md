# 实验操作手册

本文说明本项目中三类实验的运行方式、配置文件位置、参数含义和结果查看方法：

1. **纯 Python 批量算法仿真**：不启动 Mininet，适合大规模、快速统计实验。
2. **单次 Mininet SRv6 实验**：启动真实 Mininet、Linux SRv6、tc/netem 和 UDP agent，适合端到端验证。
3. **批量 Mininet 场景实验**：按配置生成多个随机断链场景，逐个启动 Mininet 实验，适合小批量真实网络仿真。

---

## 一、实验类型选择

### 1. 纯 Python 批量算法仿真

入口：

```bash
python3 scripts/random_experiments.py --config config/random_experiments.toml
```

特点：

* 不需要 root 权限。
* 不需要 Mininet、iproute2 SRv6 或 tc。
* 运行速度快，适合 100 节点、上千个随机场景的算法统计。
* 输出每个场景的最终状态、访问节点数、跳数、累计时延、断链数量、SRv6 Segment List 统计等指标。

适用场景：

* 验证算法在随机断链下的成功率。
* 统计 `temporarily_unreachable`、`partial_result` 等失败场景数量。
* 快速比较不同断链概率、节点规模、时延模型、`max_hop` 的影响。

### 2. 单次 Mininet SRv6 实验

入口：

```bash
sudo python3 -m emulation.mininet_srv6_lab --config config/mininet_srv6.toml --no-cli
```

特点：

* 启动真实 Mininet 网络。
* 在 Linux namespace 中配置 IPv6、SRv6 SID、SRv6 policy 和 tc/netem。
* 可启用 UDP agent，验证探测包是否真实到达目标节点。
* 适合少量节点下做端到端功能验证。

适用场景：

* 验证 Linux SRv6 路由、SID、解封装、UDP agent 是否工作。
* 检查 `tc netem loss 100%` 是否能模拟断链。
* 检查 `traversal_events.jsonl`、`policy_updates.jsonl` 和 `tc_updates.jsonl` 是否记录正确。

### 3. 批量 Mininet 场景实验

入口：

```bash
python3 -m emulation.mininet_batch_experiments --config config/mininet_batch_experiments.toml
```

真实运行时，需要先把配置中的 `dry_run` 改为 `false`，再使用 root 权限：

```bash
sudo python3 -m emulation.mininet_batch_experiments --config config/mininet_batch_experiments.toml
```

特点：

* 通过一个配置文件控制多个 Mininet 场景。
* 每个场景随机生成断链集合。
* 每个场景单独启动一次 `emulation.mininet_srv6_lab`，避免不同场景之间的 Mininet/tc/SRv6 状态互相污染。
* 如果某个场景出现 `temporarily_unreachable`、`partial_result` 等终止状态，会记录该场景，然后自动进入下一个场景。

适用场景：

* 需要真实 Mininet 环境，但又希望批量运行多个随机断链场景。
* 验证算法在真实 SRv6/tc 环境中的稳定性。
* 对少量或中等数量场景做端到端实验统计。

注意：100 个 Mininet 节点比较重，建议先使用 4x4 或 6x6 dry-run/真实小规模场景确认环境稳定，再扩大到 10x10。

---

## 二、环境准备

### 1. 安装 Python 包

建议在项目根目录执行：

```bash
python3 -m pip install -e ".[test]"
```

如果只运行纯 Python 算法仿真，项目本身没有额外运行时依赖。

### 2. Mininet 实验依赖

Mininet 实验需要 Linux 环境，并安装以下工具：

```bash
sudo apt update
sudo apt install -y mininet openvswitch-switch iproute2 iputils-ping tcpdump
```

检查环境：

```bash
python3 -m emulation.env_check
```

真实 Mininet 实验需要 root 权限：

```bash
sudo python3 -m emulation.mininet_srv6_lab --config config/mininet_srv6.toml --no-cli
```

---

## 三、纯 Python 批量算法仿真

### 1. 配置文件

默认配置：

```text
config/random_experiments.toml
```

当前默认是 10 x 10 网格，即 100 个仿真节点。

### 2. 运行命令

```bash
python3 scripts/random_experiments.py --config config/random_experiments.toml
```

也可以省略 `--config`，脚本默认读取 `config/random_experiments.toml`：

```bash
python3 scripts/random_experiments.py
```

### 3. 主要配置项说明

#### `[scenario]`

```toml
[scenario]
count = 2000
seed = 7
interrupt_statuses = ["temporarily_unreachable", "partial_result"]
```

参数含义：

* `count`：随机实验场景数量。比如 `2000` 表示连续运行 2000 个随机断链场景。
* `seed`：随机种子。相同种子会生成相同的随机场景，便于复现实验。
* `interrupt_statuses`：认为需要中断当前场景并进入下一个场景的终止状态。

常见状态：

* `finished`：成功完成全网遍历并返回根节点。
* `temporarily_unreachable`：当前估计拓扑中目标节点暂时不可达。
* `partial_result`：通常表示达到 hop limit，或未完成完整遍历。

说明：纯 Python 仿真中，只要引擎返回非 `running` 状态，当前场景都会结束；`interrupt_statuses` 用于在输出中标记这些场景属于 interrupted。

#### `[grid]`

```toml
[grid]
rows = 10
cols = 10
```

参数含义：

* `rows`：网格行数，也可理解为轨道面数量。
* `cols`：网格列数，也可理解为每个轨道面内的卫星数量。
* 节点总数为 `rows * cols`。

示例：

* `rows = 4, cols = 4`：16 个节点，适合调试。
* `rows = 10, cols = 10`：100 个节点，适合较大规模算法统计。

注意：当前哈密顿圈构造要求 `rows` 为偶数。

#### `[delay]`

```toml
[delay]
model = "leo"
period_slots = 8
```

参数含义：

* `model`：时延模型。
  * `"leo"`：LEO 类周期时延模型。
  * `"random"`：每条边、每个时间槽随机生成时延。
* `period_slots`：周期时延表的时间槽数量。

#### `[leo_delay]`

```toml
[leo_delay]
intra_delay = 1.0
inter_min_delay = 1.2
inter_max_delay = 3.0
```

参数含义：

* `intra_delay`：同轨道面内链路时延。
* `inter_min_delay`：跨轨道面链路最小时延。
* `inter_max_delay`：跨轨道面链路最大时延。

当 `[delay].model = "leo"` 时使用这些参数。

#### `[random_delay]`

```toml
[random_delay]
min_delay = 0.8
max_delay = 3.0
```

参数含义：

* `min_delay`：随机时延下限。
* `max_delay`：随机时延上限。

当 `[delay].model = "random"` 时使用这些参数。

#### `[failure]`

```toml
[failure]
mode = "probability"
down_probability = 0.1
down_edges_per_scenario = 18
```

参数含义：

* `mode`：随机断链模式。
  * `"probability"`：每条链路按概率独立断开。
  * `"fixed_count"`：每个场景固定随机选择指定数量的链路断开。
* `down_probability`：当 `mode = "probability"` 时，每条链路断开的概率。
* `down_edges_per_scenario`：当 `mode = "fixed_count"` 时，每个场景随机断开的链路数量。

示例 1：每条边有 10% 概率断开。

```toml
[failure]
mode = "probability"
down_probability = 0.1
```

示例 2：每个场景固定随机断开 18 条边。

```toml
[failure]
mode = "fixed_count"
down_edges_per_scenario = 18
```

#### `[traversal]`

```toml
[traversal]
max_hop = 0
alpha = 0.85
```

参数含义：

* `max_hop`：探测包最大跳数。
  * `0` 表示自动设置为 `rows * cols * 30`。
  * 如果设置为正整数，则使用该值。
* `alpha`：软重规划阈值参数。当前路径代价相对新路径变差到一定程度时，可触发重规划。值越接近 1，越容易重规划。

#### `[simulation]`

```toml
[simulation]
step_time = 1.0
```

参数含义：

* `step_time`：仿真中每跳推进的时间步长。

#### `[srv6]`

```toml
[srv6]
enabled = true
locator_prefix = "fc00:0"
base_srh_overhead_bytes = 8
per_sid_overhead_bytes = 16
```

参数含义：

* `enabled`：是否启用 SRv6 统计模型。
* `locator_prefix`：模拟 SID 前缀。
* `base_srh_overhead_bytes`：SRH 基础开销。
* `per_sid_overhead_bytes`：每个 SID 增加的开销。

注意：纯 Python 算法仿真中的 SRv6 是统计模型，不会真正调用 Linux SRv6。

#### `[output]`

```toml
[output]
base_dir = "logs/random"
run_name = ""
write_stdout = true
```

参数含义：

* `base_dir`：输出目录根路径。
* `run_name`：本次实验输出文件夹名称。为空时自动使用时间戳。
* `write_stdout`：是否在终端实时打印每个场景的结果。

### 4. 输出文件

一次运行会生成类似目录：

```text
logs/random/<run>/
  run_config.json
  summary.txt
  runs.jsonl
  policy_events.jsonl
```

文件含义：

* `run_config.json`：本次实验实际使用的配置。
* `summary.txt`：终端输出摘要，包括成功率、状态统计、平均跳数等。
* `runs.jsonl`：每个场景一行 JSON，记录该场景的最终状态和指标。
* `policy_events.jsonl`：启用 SRv6 统计时，记录每次策略更新事件。

---

## 四、单次 Mininet SRv6 实验

### 1. 配置文件

默认配置：

```text
config/mininet_srv6.toml
```

### 2. dry-run

dry-run 不启动 Mininet，只打印将要执行的命令：

```bash
python3 -m emulation.mininet_srv6_lab --config config/mininet_srv6.toml --dry-run
```

适合检查配置是否正确、路径是否能规划、SRv6 命令是否生成。

### 3. 真实运行

```bash
sudo python3 -m emulation.mininet_srv6_lab --config config/mininet_srv6.toml --no-cli
```

参数说明：

* `--no-cli`：实验运行结束后不进入 Mininet CLI，适合自动化实验。
* 不加 `--no-cli` 时，如果配置中的 `enable_cli = true`，实验结束后会进入 Mininet CLI。

### 4. 主要配置项说明

#### `[constellation]`

```toml
[constellation]
planes = 4
satellites_per_plane = 4
intra_plane_wrap = false
inter_plane_links = true
inter_plane_wrap = false
```

参数含义：

* `planes`：轨道面数量。
* `satellites_per_plane`：每个轨道面内的卫星数量。
* `intra_plane_wrap`：同一轨道面内首尾是否相连。
* `inter_plane_links`：是否启用相邻轨道面之间的横向链路。
* `inter_plane_wrap`：轨道面之间是否首尾环绕相连。

节点总数为：

```text
planes * satellites_per_plane
```

#### `[srv6]`

```toml
[srv6]
locator_prefix = "fc00:0"
decap_table = "255"
```

参数含义：

* `locator_prefix`：Node SID 的前缀。
* `decap_table`：End.DT6 解封装后查找的 Linux 路由表。

#### `[emulation]`

```toml
[emulation]
duration = 40
slot_seconds = 1
default_delay_ms = 5
enable_cli = true
```

参数含义：

* `duration`：实验总运行时间，单位秒。
* `slot_seconds`：动态拓扑和时延更新的时间步长。
* `default_delay_ms`：默认链路时延，单位毫秒。
* `enable_cli`：是否在实验结束后进入 Mininet CLI。

#### `[delay]`

```toml
[delay]
model = "propagation"
period_slots = 8
constant_delay_ms = 5
altitude_km = 550
inclination_deg = 53
min_delay_ms = 0.1
```

参数含义：

* `model`：Mininet 链路时延模型。
  * `"constant"`：固定时延。
  * `"propagation"`：基于简化轨道几何的传播时延。
* `period_slots`：周期时延槽数量。
* `constant_delay_ms`：固定时延模型下的链路时延。
* `altitude_km`：卫星轨道高度。
* `inclination_deg`：轨道倾角。
* `min_delay_ms`：传播时延下限，避免过小值。

#### `[dynamic_topology]`

```toml
[dynamic_topology]
enabled = true
model = "rotating_seam"
period_slots = 8
```

参数含义：

* `enabled`：是否启用动态拓扑断链。
* `model`：动态拓扑模型。
  * `"static"`：不额外生成动态 seam。
  * `"rotating_seam"`：不可用的跨轨道面链路随时间槽移动。
* `period_slots`：动态拓扑周期槽数量。

#### `[algorithm]`

```toml
[algorithm]
mode = "adaptive_traversal"
max_hop = 500
alpha = 0.85
validate_each_policy = true
```

参数含义：

* `mode`：实验算法模式。
  * `"adaptive_traversal"`：自适应哈密顿遍历遥测算法。
  * `"root_target"`：根节点到固定目标的 SRv6 连通性测试模式。
* `max_hop`：探测包最大跳数。
* `alpha`：软重规划阈值。
* `validate_each_policy`：每次安装 SRv6 policy 后是否执行 ping/route 验证。

#### `[observation]`

```toml
[observation]
mode = "ping"
stale_after_seconds = 2
```

参数含义：

* `mode`：链路观测方式。
  * `"configured"`：直接使用配置中的物理链路状态。
  * `"ping"`：在 Mininet namespace 中用 IPv6 ping 检测邻接链路。
* `stale_after_seconds`：DOWN 观测过期时间。过期后，链路可重新被视为未知或默认可用。

#### `[agent]`

```toml
[agent]
enabled = true
probe_packet_validation = true
udp_port = 5005
```

参数含义：

* `enabled`：是否启动每个节点的 UDP agent。
* `probe_packet_validation`：是否通过 UDP 探测包验证 SRv6 policy 是否能把包送到目标 agent。
* `udp_port`：agent 监听端口。

#### `[failure]`

```toml
[failure]
edge = [9, 5]
start = 5
end = 15
```

参数含义：

* `edge`：指定故障链路。
* `start`：故障开始时间，单位秒。
* `end`：故障结束时间，单位秒。

单次 Mininet 实验也支持多条指定断链：

```toml
[failure]
edges = [[0, 1], [5, 9], [13, 14]]
start = 0
end = 20
```

也可以通过命令行覆盖：

```bash
sudo python3 -m emulation.mininet_srv6_lab \
  --config config/mininet_srv6.toml \
  --failure-edges "0,1;5,9;13,14" \
  --failure-start 0 \
  --failure-end 20 \
  --no-cli
```

#### `[output]`

```toml
[output]
base_dir = "logs/mininet"
run_name = ""
```

参数含义：

* `base_dir`：Mininet 实验输出根目录。
* `run_name`：本次实验目录名。为空时使用时间戳。

### 5. 输出文件

单次 Mininet 实验输出目录示例：

```text
logs/mininet/<run>/
  run_config.json
  mininet_srv6.log
  policy_updates.jsonl
  tc_updates.jsonl
  traversal_events.jsonl
  agent_r<node>.jsonl
  agent_r<node>.stderr.log
```

文件含义：

* `run_config.json`：本次运行的完整配置。
* `mininet_srv6.log`：执行过的 Linux 命令和输出。
* `policy_updates.jsonl`：SRv6 policy 更新记录。
* `tc_updates.jsonl`：tc/netem 链路时延和 loss 更新记录。
* `traversal_events.jsonl`：探测包遍历事件、状态、路径、已访问节点。
* `agent_r<node>.jsonl`：节点 agent 收到的 UDP 探测包和本地链路观测。
* `agent_r<node>.stderr.log`：agent 标准错误日志。

---

## 五、批量 Mininet 场景实验

### 1. 配置文件

默认配置：

```text
config/mininet_batch_experiments.toml
```

当前配置默认是 10 x 10，即 100 个 Mininet 节点，并且默认 `dry_run = true`。

### 2. dry-run

```bash
python3 -m emulation.mininet_batch_experiments --config config/mininet_batch_experiments.toml
```

dry-run 用于检查每个场景的命令生成和随机断链选择，不启动真实 Mininet。

### 3. 真实运行

先修改配置：

```toml
[execution]
dry_run = false
```

然后运行：

```bash
sudo python3 -m emulation.mininet_batch_experiments --config config/mininet_batch_experiments.toml
```

### 4. 主要配置项说明

#### `[scenario]`

```toml
[scenario]
count = 20
seed = 7
interrupt_statuses = ["temporarily_unreachable", "partial_result"]
```

参数含义：

* `count`：Mininet 场景数量。
* `seed`：随机种子。
* `interrupt_statuses`：如果某个场景最终状态属于该列表，则记为 interrupted，然后启动下一个场景。

#### `[execution]`

```toml
[execution]
lab_config = "config/mininet_srv6.toml"
dry_run = true
continue_on_runner_error = true
```

参数含义：

* `lab_config`：每个场景调用的单次 Mininet 实验基础配置文件。
* `dry_run`：是否只打印命令而不启动 Mininet。
* `continue_on_runner_error`：单个场景进程异常退出时，是否继续运行后续场景。

建议：

* 初次配置时保持 `dry_run = true`。
* 确认命令和断链数量无误后，再改为 `false` 并使用 `sudo` 运行。

#### `[constellation]`

```toml
[constellation]
planes = 10
satellites_per_plane = 10
```

参数含义：

* `planes`：轨道面数量。
* `satellites_per_plane`：每个轨道面的卫星数量。
* 节点总数为 `planes * satellites_per_plane`。

示例：

* `4 x 4`：16 节点，适合调试。
* `6 x 6`：36 节点，适合中等测试。
* `10 x 10`：100 节点，适合大规模 Mininet 实验，但资源开销明显更高。

#### `[failure]`

```toml
[failure]
mode = "probability"
down_probability = 0.1
down_edges_per_scenario = 18
start = 0
end = 120
```

参数含义：

* `mode`：随机断链方式。
  * `"probability"`：每条物理链路按概率独立断开。
  * `"fixed_count"`：每个场景固定随机选择若干条链路断开。
* `down_probability`：概率断链模式下，每条链路断开的概率。
* `down_edges_per_scenario`：固定数量模式下，每个场景断开的链路数量。
* `start`：随机断链开始时间。
* `end`：随机断链结束时间。

示例：每个场景固定断 18 条链路。

```toml
[failure]
mode = "fixed_count"
down_edges_per_scenario = 18
start = 0
end = 120
```

#### 其他配置

批量 Mininet 配置中的以下表和单次 Mininet 含义一致：

* `[emulation]`
* `[delay]`
* `[dynamic_topology]`
* `[algorithm]`
* `[observation]`
* `[agent]`
* `[output]`

区别是：批量 runner 会把这些参数转成命令行参数，逐个传给 `emulation.mininet_srv6_lab`。

### 5. 输出文件

批量 Mininet 输出目录示例：

```text
logs/mininet-batch/<batch>/
  batch_config.json
  summary.txt
  scenarios.jsonl
  <scenario>.stdout.log
  <scenario>/
    run_config.json
    mininet_srv6.log
    policy_updates.jsonl
    tc_updates.jsonl
    traversal_events.jsonl
```

文件含义：

* `batch_config.json`：批量实验总配置。
* `summary.txt`：批量场景摘要。
* `scenarios.jsonl`：每个场景一行 JSON，记录状态、断链集合、访问节点数、跳数等。
* `<scenario>.stdout.log`：该场景子进程的 stdout/stderr。
* `<scenario>/`：该场景自己的单次 Mininet 实验输出目录。

---

## 六、常见实验修改示例

### 1. 把纯 Python 算法仿真改为 1000 个场景

修改 `config/random_experiments.toml`：

```toml
[scenario]
count = 1000
```

运行：

```bash
python3 scripts/random_experiments.py
```

### 2. 把纯 Python 仿真改为每个场景固定断 25 条链路

```toml
[failure]
mode = "fixed_count"
down_edges_per_scenario = 25
```

### 3. 把 Mininet 批量实验先缩小到 4x4 调试

修改 `config/mininet_batch_experiments.toml`：

```toml
[constellation]
planes = 4
satellites_per_plane = 4

[scenario]
count = 2

[execution]
dry_run = true
```

运行：

```bash
python3 -m emulation.mininet_batch_experiments --config config/mininet_batch_experiments.toml
```

### 4. 单次 Mininet 实验指定多条断链

```bash
sudo python3 -m emulation.mininet_srv6_lab \
  --config config/mininet_srv6.toml \
  --failure-edges "0,1;5,9;13,14" \
  --failure-start 0 \
  --failure-end 20 \
  --no-cli
```

### 5. 关闭 agent，加快 Mininet 实验

```bash
sudo python3 -m emulation.mininet_srv6_lab \
  --config config/mininet_srv6.toml \
  --disable-agents \
  --no-probe-packet-validation \
  --no-cli
```

说明：

* 关闭 agent 后，不再验证 UDP 探测包是否真实到达目标节点。
* 但仍可以观察 SRv6 policy、tc 更新和 traversal 事件。

---

## 七、结果状态解释

### `finished`

表示探测包完成全网遍历，并返回根节点。该场景视为成功。

### `temporarily_unreachable`

表示在当前估计拓扑中，下一待测节点暂时不可达。通常由断链过多、关键链路断开或观测到 DOWN 链路导致。

### `partial_result`

表示实验只得到部分结果，常见原因是达到 `max_hop` 限制。该状态用于发现潜在环路、异常绕行或过高转发代价。

### `runner_error`

仅批量 Mininet runner 中出现，表示某个场景的子进程异常退出。需要查看对应的 `<scenario>.stdout.log` 和场景目录下的 `mininet_srv6.log`。

---

## 八、建议实验流程

推荐按以下顺序推进：

1. 先运行纯 Python 100 节点批量算法仿真：

```bash
python3 scripts/random_experiments.py
```

2. 根据结果调整断链概率、固定断链数量、`max_hop` 和 `alpha`。

3. 用单次 4x4 Mininet 验证 SRv6/tc/agent 环境：

```bash
sudo python3 -m emulation.mininet_srv6_lab --config config/mininet_srv6.toml --no-cli
```

4. 用 Mininet batch 的 4x4 dry-run 和真实运行做小规模批量验证。

5. 最后再把 Mininet batch 扩大到 10x10，即 100 节点。

这样可以先用轻量仿真找到合适参数，再用真实 Mininet 环境验证关键场景，避免直接运行 100 节点 Mininet 时排查成本过高。
